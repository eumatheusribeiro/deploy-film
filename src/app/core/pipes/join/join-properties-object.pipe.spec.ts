import { JoinPropertiesObjectPipe } from './join-properties-object.pipe';

describe('JoinPropertiesObjectPipe', () => {
  it('create an instance', () => {
    const pipe = new JoinPropertiesObjectPipe();
    expect(pipe).toBeTruthy();
  });
});
